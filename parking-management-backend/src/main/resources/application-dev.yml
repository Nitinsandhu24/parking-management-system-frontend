spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    clean-on-validation-error: true

logging:
  level:
    com.parking: DEBUG
    org.springframework.security: DEBUG
